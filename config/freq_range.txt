# ============================================================
# fuyun CPU 频率范围配置 (小/中/大核 min/max 可调)
# 安装时复制到 /sdcard/Android/yc/uperf/，可随时编辑，保存后立即生效。
#
# 值含义:
#   0       = 动态，不限制/不锁频
#   非 0    = 必须是当前 SoC 支持频点 (kHz)，例如 1804800 = 1.8048GHz
#   min=max = 锁频
#
# 支持频点见模块内 script/freq_table_8p.txt / freq_table_8g2.txt。
# 非法值会被忽略并记录到日志，不会影响其他簇。
# ============================================================

# 总开关: 1=启用本配置 0=停用 (停用后完全恢复动态)
FREQ_RANGE_ENABLE=0

# 小核 (8+ 为 CPU0-3, 8G2 为 CPU0-2)
LITTLE_MIN=0
LITTLE_MAX=0

# 中核 (8+ 为 CPU4-6, 8G2 为 CPU3-6)
MID_MIN=0
MID_MAX=0

# 大核 (CPU7)
BIG_MIN=0
BIG_MAX=0
