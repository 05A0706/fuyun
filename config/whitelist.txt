# ============================================================
# fuyun 白名单 (whitelist.txt) - 内存回收 / 辅助调速器 / Doze 三合一
# 分区: [mem] 回收白名单 (支持 com.xxx.* 前缀通配)
#       [idle_gov] 调速器白名单 (支持前缀通配, 前台命中永不降频)
#       [doze] 息屏 Doze 推送白名单 (仅豁免不杀应用)
# 每行一个包名, # 开头为注释, 重启保留; WebUI 三卡片可视化管理
# ============================================================

[mem]
# fuyun 内存回收白名单: 每行一个包名, # 开头为注释, 重启保留
# 白名单内的应用不会被回收/清理
# (系统应用 / 桌面 / 输入法已内置保护, 无需添加)

com.tencent.mm
com.tencent.mobileqq
com.tencent.tim
com.coolapk.market
com.android.mms
com.android.email
com.google.android.apps.messaging
com.android.cellbroadcastreceiver

[idle_gov]
# fuyun 辅助调速器白名单: 每行一个包名, # 开头为注释, 重启保留
# 支持前缀通配: 例如 com.tencent.* 排除所有腾讯应用
# 前台应用命中白名单时永不压制频率
# 注: 视频解码/地图渲染等持续高 CPU 占用应用会被自动豁免, 通常无需手动添加

[doze]
# fuyun 息屏 Doze 白名单: 每行一个包名, # 开头为注释, 重启保留
# 白名单内的应用在深度 Doze 时仍可接收推送/消息
com.tencent.mm # 微信
com.tencent.mobileqq # QQ
com.tencent.tim # Tim
com.coolapk.market # 酷安
com.android.mms # 短信
com.android.email # 邮件
com.google.android.apps.messaging # 谷歌FCM推送
com.android.cellbroadcastreceiver # 紧急广播
